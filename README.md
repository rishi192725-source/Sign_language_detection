# Sign Language Detection App

This project detects sign language letters from uploaded images using a trained YOLO model and Streamlit UI.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py